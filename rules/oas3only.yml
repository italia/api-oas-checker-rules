rules:
  no-swagger-2:
    description: >-
      Swagger 2 files are not advisable. Use OpenAPI >= 3.0
    given: $
    severity: warn
    recommended: true
    type: style
    formats:
    - oas2
    then:
      field: "swagger"
      function: falsy

